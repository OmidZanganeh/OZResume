# Agent instructions (Cursor)

1. **Orientation:** Open **`CODEBASE_MAP.md`** first. Use it to find the right files. Do not read the whole tree unless the task requires it.
2. **Scoped reads:** Prefer `@CODEBASE_MAP.md` plus **only** the files the map points to for the current task.
3. **Updates:** If you add a route, API, or major feature, **update `CODEBASE_MAP.md`** in the same change when practical.
