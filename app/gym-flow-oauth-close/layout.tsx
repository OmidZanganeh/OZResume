import type { ReactNode } from 'react';

export const metadata = {
  title: 'Gym Flow — sign-in',
  robots: { index: false, follow: false },
};

export default function GymFlowOauthCloseLayout({ children }: { children: ReactNode }) {
  return children;
}
